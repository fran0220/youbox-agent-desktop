import { PRODUCT_BRAND_SHORT } from '@craft-agent/shared/product-identity'

/** CLI program name shown in help and user-facing errors. */
export const CLI_PROGRAM_NAME = 'ocn'

/** One-line description for --help. */
export const CLI_DESCRIPTION = `Terminal client for ${PRODUCT_BRAND_SHORT} Agent server`

/** Temp directory prefix for integration validate runs. */
export const CLI_VALIDATE_TMP_PREFIX = 'ocn-validate-'
